# 2026-09-20 연구 2건 — 진입기준 / 슬롯배분

두 연구 모두 **READ-ONLY** 로 수행했다. production/config 무수정이고, 이 커밋은
연구 기록만 담는다 — 전략 동작은 한 줄도 바뀌지 않는다.

기준 커밋: `c05ccb2` (main-MACD2, N1+C1 + MarketData pandas3 hotfix).
기준 성능: N1+C1 = 158건 / 78일 438.6268 / PF 2.658 / MDD −8.91.

## 1. `research_20260920_entry_filter/` — **REJECT**

"H50/N1 이 거른 플래그 중 좋았던 것을 넣고, 들어갔는데 나쁜 것을 빼는" 진입필터.

- 551개 플래그 전량을 원장화하고, 각각 "이 플래그 하나만 진입했다면"을 독립 측정했다.
- VWAP veto 가 거른 71건은 독립 측정에서 평균 +1.57 / 승률 64.8% 로 **확실히 좋았다**
  (승인 플래그는 +0.84 / 47.6%).
- **그러나 실제로 넣으면 전 구간 악화**(−7 ~ −47). 하루 3회 한도 때문에 진입 추가는
  제로섬이고, 먼저 들어온 플래그가 뒤의 더 좋은 자리를 먹는다.
- 제거측은 진입시점 특징의 판별력이 AUC 0.44~0.53(동전 던지기). 유일한 양수 후보
  R1(+43.08)은 plateau 실패·부트스트랩 유의성 실패·최근 구간 무근거로 기각.

→ **진입기준 무변경 유지.** 다만 "3회 한도가 병목"이라는 사실을 확인했고, 그것이
다음 연구의 출발점이 됐다.

## 2. `research_20260920_slot_allocation/` — **ADOPT 권고 (미구현)**

같은 3회를 누구에게 얼마나 주느냐.

- 레버리지와 배분을 분리하기 위해 **실현 총노출을 기준(157.28)에 맞춘 뒤**
  같은 노출의 무배분 레버리지와 비교했다.
- 정체는 slot3 전체가 아니라 **오전(09:00~11:00) 3번째 진입** 8건이었다.
  오후 slot3 은 오히려 건드리면 안 된다(30일 −11.50).
- **슬롯을 비우면 손해**(−20.50) — 그 자리가 더 나쁜 후보로 넘어간다.
  **진입은 하되 자금을 주지 않는 것**(+51.93)이 핵심이다.
- 권고안 P2: 오전 3번째 진입 사이징 배수를 production 상수
  `X2LITE_SIZING_MIN_MULT`(0.25)로 낮추고 slot1·2 를 ×1.05 로 올려 노출을 맞춘다.
  → 78일 +54.39 / 30일 +4.11 / PF 2.658→2.789 / MDD −8.91→−8.38 /
  5분할 전부 양수 / WF 5승1패 / 진입집합 불변 / 러너 손상 0.
- LOO(8건 각각) 최악 +36.82, 월 제외 +12.5~+15.3, 부트스트랩 >0 96.0%.

→ **미구현.** 채택 시 건드릴 곳은 `position_sizing` 한 군데이며 N1/C1 로직은 무변경.
**약점: 대상이 8건뿐이다.** 실계좌 반영 후 1~2개월 추가 관측으로 재확인할 것.

## 재현

각 폴더의 `HOWTO_REPRODUCE.md` 참조. 연구 엔진(`hengine5.py`)은 production 사본에
훅만 덧붙인 것이며, 훅 미지정 시 앵커(N1 401.0853 / N1+C1 438.6268 / H50 230.6116)를
그대로 재현한다.
